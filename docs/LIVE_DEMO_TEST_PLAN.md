# Final live demo acceptance — 2026-09-16

Bradbury chain 4221; contract `0xa73c0183e2e3605bbd5013abcb9c5683f7db1b4a`. The 723-byte pinned `genlayer-js@1.1.8` fixture produced genuine finalized LOW `WARRANTED` and HIGH `NOT_WARRANTED` adjudications. URL, raw identity, hash, requirements `[]`, implementation and validator architecture were shared; purpose and risk changed.

Fresh MetaMask warrant `warrant-4905a143-4d22-4603-9763-c15e87166a47` followed `PENDING → PENDING_AI → WARRANTED`. All three outer receipts succeeded; all three GenLayer writes finalized `AGREE / FINISHED_WITH_RETURN`. Cold reload without signer/tracker state passed. The frontend reads authoritative contract state.

See [the final proof bundle](submission-proof/README.md), [judge demo](SUBMISSION_DEMO.md) and [verification](submission-proof/verification.json). Do not rerun the experiment or create replacement warrants for the demo.

## Historical record — superseded

The following earlier state is preserved as historical evidence. It is not the current result; `INCONCLUSIVE` from that attempt did not establish an LLM judgment.

# Live Demo Test Plan

Currently BLOCKED due to `INCONCLUSIVE` result on `networks.mdx` fixture.
