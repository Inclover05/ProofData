# Official GenLayer Skills Installation

## Source Information
- **Source repository:** https://github.com/genlayerlabs/skills
- **Installed commit SHA:** 195deb417c2ac4a90dd23429a0c3940bde80389a
- **Installation date:** 2026-09-14

## Installed Skills
The following skills were installed from `plugins/genlayer-dev/skills/`:
- write-contract (installed as `genlayer-official-write-contract`)
- genlayer-cli (installed as `genlayer-official-genlayer-cli`)
- genvm-lint (installed as `genlayer-official-genvm-lint`)
- direct-tests (installed as `genlayer-official-direct-tests`)
- integration-tests (installed as `genlayer-official-integration-tests`)

## Workspace Location
- `.agent/skills/`

## Configuration & Support
- **Antigravity discovery:** PASS (Workspace skills are natively discovered dynamically when walking up to the repo root)
- **Bradbury support found:** YES (via `testnet_bradbury` alias in `gltest.config.yaml` using funded accounts)
- **Docs MCP status:** NOT CONFIGURED (The repository contains an MCP configuration, but it was not activated. The required configuration for Antigravity would be `{"mcpServers": {"genlayer-docs": {"serverUrl": "https://docs-mcp.genlayer.com/sse"}}}` inside `~/.gemini/config/mcp_config.json`)
