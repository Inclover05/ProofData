networks:
  default: localnet

  localnet:
    url: "http://127.0.0.1:4000/api"

  studionet: {}

  testnet_bradbury:
    accounts:
      - "${ACCOUNT_PRIVATE_KEY_1}"
    
paths:
  contracts: "contracts"

environment: .env
